---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: clusterregistrations.tmc.kcp.io
spec:
  group: tmc.kcp.io
  names:
    kind: ClusterRegistration
    listKind: ClusterRegistrationList
    plural: clusterregistrations
    singular: clusterregistration
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRegistration represents a physical cluster registered
          with the TMC for workload management.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRegistrationSpec holds the desired state of the ClusterRegistration.
            properties:
              capacity:
                description: Capacity defines the advertised capacity of the cluster
                properties:
                  cpu:
                    description: CPU is the total CPU capacity of the cluster in milliCPU
                    format: int64
                    type: integer
                  maxPods:
                    description: MaxPods is the maximum number of pods that can be
                      scheduled on this cluster
                    format: int32
                    type: integer
                  memory:
                    description: Memory is the total memory capacity of the cluster
                      in bytes
                    format: int64
                    type: integer
                type: object
              clusterEndpoint:
                description: ClusterEndpoint defines how to connect to the cluster
                properties:
                  caBundle:
                    description: CABundle contains the certificate authority bundle
                      for the cluster
                    format: byte
                    type: string
                  serverURL:
                    description: ServerURL is the URL of the Kubernetes API server
                    format: uri
                    type: string
                  tlsConfig:
                    description: TLSConfig contains additional TLS configuration
                    properties:
                      insecureSkipVerify:
                        default: false
                        description: InsecureSkipVerify controls whether to skip certificate
                          verification
                        type: boolean
                    type: object
                required:
                - serverURL
                type: object
              location:
                description: Location specifies the geographical location of the cluster
                type: string
            required:
            - clusterEndpoint
            - location
            type: object
          status:
            description: ClusterRegistrationStatus communicates the observed state
              of the ClusterRegistration.
            properties:
              allocatedResources:
                description: AllocatedResources tracks the resources currently allocated
                  on this cluster
                properties:
                  cpu:
                    description: CPU usage in milliCPU
                    format: int64
                    type: integer
                  memory:
                    description: Memory usage in bytes
                    format: int64
                    type: integer
                  pods:
                    description: Pod count
                    format: int32
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the cluster's state
                items:
                  description: Condition defines an observation of a object operational
                    state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              lastHeartbeat:
                description: LastHeartbeat is the timestamp of the last successful
                  cluster heartbeat
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
