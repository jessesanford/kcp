---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: clusterregistrations.tmc.kcp.io
spec:
  group: tmc.kcp.io
  names:
    kind: ClusterRegistration
    listKind: ClusterRegistrationList
    plural: clusterregistrations
    singular: clusterregistration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterRegistration represents a minimal stub for cluster registration.
          This is a placeholder for the placement engine interface.
          Full implementation will be provided in a follow-up PR.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRegistrationSpec defines minimal cluster information
              for placement decisions.
            properties:
              location:
                description: Location specifies the geographical location of the cluster
                type: string
            type: object
          status:
            description: ClusterRegistrationStatus represents the minimal status needed
              by placement engine.
            properties:
              ready:
                description: Ready indicates if the cluster is available for placement
                type: boolean
            type: object
        type: object
    served: true
    storage: true
