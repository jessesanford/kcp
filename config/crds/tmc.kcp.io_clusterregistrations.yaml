---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: clusterregistrations.tmc.kcp.io
spec:
  group: tmc.kcp.io
  names:
    categories:
    - kcp
    kind: ClusterRegistration
    listKind: ClusterRegistrationList
    plural: clusterregistrations
    singular: clusterregistration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.location
      name: Location
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterRegistration represents a physical Kubernetes cluster registered with TMC.
          It tracks cluster health, capabilities, and connectivity for workload placement.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the ClusterRegistration.
            properties:
              acceptedWorkloadTypes:
                description: |-
                  AcceptedWorkloadTypes specifies what types of workloads this cluster
                  can accept. If empty, all workload types are accepted.
                items:
                  description: WorkloadType represents a Kubernetes workload type
                  properties:
                    apiVersion:
                      description: APIVersion is the API version of the workload type.
                      type: string
                    kind:
                      description: Kind is the kind of the workload type.
                      type: string
                  required:
                  - apiVersion
                  - kind
                  type: object
                type: array
              capabilities:
                description: |-
                  Capabilities describes the capabilities and resources available
                  on this cluster for placement decisions.
                properties:
                  hasLoadBalancer:
                    description: HasLoadBalancer indicates if the cluster supports
                      LoadBalancer services.
                    type: boolean
                  hasPersistentStorage:
                    description: HasPersistentStorage indicates if the cluster supports
                      persistent volumes.
                    type: boolean
                  networkPolicies:
                    description: NetworkPolicies indicates if the cluster supports
                      NetworkPolicies.
                    type: boolean
                  supportedStorageClasses:
                    description: SupportedStorageClasses lists the available storage
                      classes.
                    items:
                      type: string
                    type: array
                type: object
              kubeConfigSecretRef:
                description: |-
                  KubeConfigSecretRef references a Secret containing the kubeconfig
                  needed to connect to this cluster. The Secret must be in the same
                  workspace as the ClusterRegistration.
                properties:
                  key:
                    description: |-
                      Key is the key in the secret that contains the kubeconfig.
                      Defaults to "kubeconfig" if not specified.
                    type: string
                  name:
                    description: Name is the name of the secret.
                    type: string
                required:
                - name
                type: object
              location:
                description: |-
                  Location is the geographical or logical location of this cluster.
                  This is used for placement decisions and policy enforcement.
                  Examples: "us-west-1", "europe", "edge", "datacenter-1"
                type: string
            required:
            - location
            type: object
          status:
            description: Status defines the observed state of the ClusterRegistration.
            properties:
              clusterInfo:
                description: |-
                  ClusterInfo contains information about the cluster's Kubernetes version
                  and other relevant details.
                properties:
                  kubernetesVersion:
                    description: KubernetesVersion is the version of Kubernetes running
                      on the cluster.
                    type: string
                  nodeCount:
                    description: NodeCount is the number of nodes in the cluster.
                    format: int32
                    type: integer
                  platform:
                    description: |-
                      Platform describes the platform/provider of the cluster.
                      Examples: "eks", "gke", "aks", "kind", "k3s"
                    type: string
                type: object
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  cluster's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastHeartbeat:
                description: LastHeartbeat is the last time the cluster reported its
                  health status.
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of cluster registration.
                type: string
              resourceSummary:
                description: ResourceSummary provides a summary of available resources
                  on this cluster.
                properties:
                  availableCPU:
                    description: AvailableCPU is the total available CPU in the cluster.
                    type: string
                  availableMemory:
                    description: AvailableMemory is the total available memory in
                      the cluster.
                    type: string
                  availableStorage:
                    description: AvailableStorage is the total available storage in
                      the cluster.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
