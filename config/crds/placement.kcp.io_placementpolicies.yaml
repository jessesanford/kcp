---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: placementpolicies.placement.kcp.io
spec:
  group: placement.kcp.io
  names:
    categories:
    - kcp
    kind: PlacementPolicy
    listKind: PlacementPolicyList
    plural: placementpolicies
    singular: placementpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.strategy
      name: Strategy
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.placedReplicas
      name: Placed
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PlacementPolicy defines how workloads should be placed across SyncTargets.
          It provides declarative scheduling policies for workload distribution based on
          various constraints, preferences, and strategies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired placement behavior for workloads.
            properties:
              affinityRules:
                description: |-
                  AffinityRules specify preferences for co-location or separation of workloads.
                  They influence but don't mandate placement decisions.
                properties:
                  workloadAffinity:
                    description: |-
                      WorkloadAffinity specifies preferences for co-locating with other workloads.
                      Higher weights indicate stronger preferences.
                    items:
                      description: |-
                        WorkloadAffinityTerm defines affinity or anti-affinity with other workloads.
                        It combines workload selection criteria with topology requirements and preference weights.
                      properties:
                        labelSelector:
                          description: LabelSelector identifies other workloads to
                            have affinity with.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        topologyKey:
                          description: |-
                            TopologyKey specifies the scope for the affinity rule.
                            Workloads are considered co-located if they share the same value for this key.
                          type: string
                        weight:
                          description: |-
                            Weight associated with matching the corresponding affinityTerm.
                            Valid values are 1-100. Higher weights have stronger influence on placement.
                          format: int32
                          maximum: 100
                          minimum: 1
                          type: integer
                      required:
                      - labelSelector
                      - topologyKey
                      type: object
                    type: array
                  workloadAntiAffinity:
                    description: |-
                      WorkloadAntiAffinity specifies preferences for avoiding co-location with other workloads.
                      Higher weights indicate stronger avoidance preferences.
                    items:
                      description: |-
                        WorkloadAffinityTerm defines affinity or anti-affinity with other workloads.
                        It combines workload selection criteria with topology requirements and preference weights.
                      properties:
                        labelSelector:
                          description: LabelSelector identifies other workloads to
                            have affinity with.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        topologyKey:
                          description: |-
                            TopologyKey specifies the scope for the affinity rule.
                            Workloads are considered co-located if they share the same value for this key.
                          type: string
                        weight:
                          description: |-
                            Weight associated with matching the corresponding affinityTerm.
                            Valid values are 1-100. Higher weights have stronger influence on placement.
                          format: int32
                          maximum: 100
                          minimum: 1
                          type: integer
                      required:
                      - labelSelector
                      - topologyKey
                      type: object
                    type: array
                type: object
              locationSelectors:
                description: |-
                  LocationSelectors define criteria for selecting eligible SyncTargets.
                  Multiple selectors are OR'd together.
                items:
                  description: |-
                    LocationSelector defines criteria for selecting eligible SyncTargets.
                    It supports direct name selection, label-based selection, and cell-based selection.
                  properties:
                    cellSelector:
                      description: |-
                        CellSelector enables selection based on KCP cell membership.
                        Provides cell-level granularity for location selection.
                      properties:
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            MatchLabels specifies labels that cells must have for their SyncTargets to be eligible.
                            All specified labels must match exactly.
                          type: object
                        requiredDuringScheduling:
                          description: |-
                            RequiredDuringScheduling specifies cell names that must be available during scheduling.
                            Placement will fail if none of these cells are available.
                          items:
                            type: string
                          type: array
                      type: object
                    labelSelector:
                      description: |-
                        LabelSelector for matching SyncTargets by labels.
                        Only SyncTargets matching all specified labels are eligible.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    name:
                      description: |-
                        Name directly specifies a SyncTarget by name.
                        Takes precedence over other selection methods if specified.
                      type: string
                  type: object
                type: array
              replicas:
                description: |-
                  Replicas specifies the desired number of replicas across all locations.
                  If not specified, defaults are applied based on the strategy.
                format: int32
                minimum: 0
                type: integer
              resourceRequirements:
                description: |-
                  ResourceRequirements specify the resource needs for each replica.
                  Used by the scheduler to ensure sufficient capacity exists.
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits specify the maximum resources each replica is allowed to consume.
                      Used for capacity planning and resource quotas.
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests specify the minimum resources required for each replica.
                      The scheduler ensures target locations can satisfy these requirements.
                    type: object
                type: object
              spreadConstraints:
                description: |-
                  SpreadConstraints control how replicas are distributed across topology domains.
                  They ensure replicas are spread according to specified rules.
                items:
                  description: |-
                    SpreadConstraint defines how replicas should be distributed across topology domains.
                    It ensures workloads are spread according to failure domain or other topology considerations.
                  properties:
                    maxSkew:
                      description: |-
                        MaxSkew describes the maximum allowed difference between the number of replicas
                        in any two topology domains. Must be greater than zero.
                      format: int32
                      minimum: 1
                      type: integer
                    topologyKey:
                      description: |-
                        TopologyKey is the key of SyncTarget labels that defines the topology domain.
                        Replicas will be spread across different values of this key.
                      type: string
                    whenUnsatisfiable:
                      description: |-
                        WhenUnsatisfiable indicates how to deal with a replica if it doesn't satisfy
                        the spread constraint.
                      enum:
                      - DoNotSchedule
                      - ScheduleAnyway
                      type: string
                  required:
                  - maxSkew
                  - topologyKey
                  - whenUnsatisfiable
                  type: object
                type: array
              strategy:
                description: Strategy defines the placement approach to use for workload
                  distribution.
                enum:
                - Singleton
                - HighAvailability
                - Spread
                - Binpack
                type: string
              targetWorkload:
                description: |-
                  TargetWorkload identifies which workloads this policy applies to.
                  It can match by API version, kind, name, or label selector.
                properties:
                  apiVersion:
                    description: APIVersion of the target workload resources.
                    type: string
                  kind:
                    description: Kind of the target workload resources.
                    type: string
                  labelSelector:
                    description: |-
                      LabelSelector for matching workloads by labels.
                      If specified, only workloads matching the selector are targeted.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  name:
                    description: |-
                      Name specifies a specific workload by name. If empty, matches all workloads
                      of the specified kind that match other criteria.
                    type: string
                required:
                - apiVersion
                - kind
                type: object
              tolerations:
                description: |-
                  Tolerations allow placement on SyncTargets with matching taints.
                  Similar to Kubernetes pod tolerations but for location-level taints.
                items:
                  description: |-
                    Toleration allows placement on SyncTargets with matching taints.
                    Similar to Kubernetes tolerations but applied at the SyncTarget level.
                  properties:
                    effect:
                      description: |-
                        Effect indicates which taint effect to tolerate.
                        Empty means tolerate all effects.
                      type: string
                    key:
                      description: Key is the taint key that the toleration matches.
                      type: string
                    operator:
                      description: |-
                        Operator represents the key-value relationship.
                        Valid operators are "Equal" and "Exists".
                      enum:
                      - Equal
                      - Exists
                      type: string
                    value:
                      description: |-
                        Value is the taint value the toleration matches.
                        Only required if Operator is "Equal".
                      type: string
                  required:
                  - key
                  type: object
                type: array
            required:
            - strategy
            - targetWorkload
            type: object
          status:
            description: Status reflects the current state of the placement policy.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the placement policy's state.
                  Standard condition types include "Scheduled" and "ConstraintsSatisfied".
                items:
                  description: Condition defines an observation of a object operational
                    state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              lastScheduleTime:
                description: LastScheduleTime records when placement was last attempted
                  by the scheduler.
                format: date-time
                type: string
              phase:
                description: Phase indicates the current lifecycle phase of the placement
                  policy.
                enum:
                - Pending
                - Scheduling
                - Scheduled
                - Failed
                type: string
              placedReplicas:
                description: PlacedReplicas is the number of replicas successfully
                  placed across all locations.
                format: int32
                type: integer
              placements:
                description: |-
                  Placements contains the specific placement decisions made by the scheduler.
                  Each entry represents replicas placed on a specific SyncTarget.
                items:
                  description: |-
                    PlacementDecision represents a specific placement made by the scheduler.
                    It records which SyncTarget received replicas and why.
                  properties:
                    locationName:
                      description: LocationName is the name of the SyncTarget where
                        replicas are placed.
                      type: string
                    reason:
                      description: |-
                        Reason provides a human-readable explanation for why this placement was made.
                        Used for debugging and policy tuning.
                      type: string
                    replicas:
                      description: Replicas is the number of replicas placed on this
                        SyncTarget.
                      format: int32
                      type: integer
                  required:
                  - locationName
                  - replicas
                  type: object
                type: array
            required:
            - placedReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
