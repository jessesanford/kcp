---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: workloadplacements.tmc.kcp.io
spec:
  group: tmc.kcp.io
  names:
    kind: WorkloadPlacement
    listKind: WorkloadPlacementList
    plural: workloadplacements
    singular: workloadplacement
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          WorkloadPlacement represents a minimal stub for workload placement requests.
          This is a placeholder for the placement engine interface.
          Full implementation will be provided in a follow-up PR.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadPlacementSpec defines minimal placement requirements.
            properties:
              clusterSelector:
                description: ClusterSelector specifies criteria for cluster selection
                properties:
                  clusterNames:
                    description: ClusterNames explicitly lists cluster names to consider
                    items:
                      type: string
                    type: array
                  labelSelector:
                    description: LabelSelector selects clusters based on labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  locationSelector:
                    description: LocationSelector selects clusters in specific locations
                    items:
                      type: string
                    type: array
                type: object
              numberOfClusters:
                description: NumberOfClusters specifies how many clusters to select
                format: int32
                type: integer
            type: object
          status:
            description: WorkloadPlacementStatus represents minimal placement status.
            properties:
              phase:
                description: Phase indicates the current phase of placement
                type: string
            type: object
        type: object
    served: true
    storage: true
