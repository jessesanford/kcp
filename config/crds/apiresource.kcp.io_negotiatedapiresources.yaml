---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: negotiatedapiresources.apiresource.kcp.io
spec:
  group: apiresource.kcp.io
  names:
    categories:
    - kcp
    kind: NegotiatedAPIResource
    listKind: NegotiatedAPIResourceList
    plural: negotiatedapiresources
    singular: negotiatedapiresource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.locationRef.name
      name: Location
      type: string
    - jsonPath: .spec.api
      name: API
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NegotiatedAPIResource represents a negotiated API resource capability
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NegotiatedAPIResourceSpec defines the negotiation requirements
            properties:
              api:
                description: API specifies the API resource to negotiate
                properties:
                  group:
                    description: Group of the API
                    type: string
                  kind:
                    description: Kind of the resource
                    type: string
                  resource:
                    description: Resource name (plural)
                    type: string
                  version:
                    description: Version of the API
                    type: string
                required:
                - kind
                - resource
                - version
                type: object
              locationRef:
                description: LocationRef references the target location
                properties:
                  name:
                    description: Name of the SyncTarget
                    type: string
                  namespace:
                    description: Namespace if applicable
                    type: string
                required:
                - name
                type: object
              requirements:
                description: Requirements for the API resource
                properties:
                  maxVersion:
                    description: MaxVersion supported
                    type: string
                  minVersion:
                    description: MinVersion required
                    type: string
                  requiredFields:
                    description: RequiredFields that must be present
                    items:
                      type: string
                    type: array
                  requiredVerbs:
                    description: RequiredVerbs that must be supported
                    items:
                      type: string
                    type: array
                type: object
            required:
            - api
            - locationRef
            type: object
          status:
            description: NegotiatedAPIResourceStatus represents the negotiation status
            properties:
              compatibilityScore:
                description: CompatibilityScore (0-100)
                format: int32
                type: integer
              conditions:
                description: Conditions of negotiation
                items:
                  description: Condition defines an observation of a object operational
                    state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              negotiatedVersion:
                description: NegotiatedVersion that was agreed upon
                type: string
              observedGeneration:
                description: ObservedGeneration
                format: int64
                type: integer
              phase:
                description: Phase of negotiation
                enum:
                - Pending
                - Negotiating
                - Compatible
                - Incompatible
                - Failed
                type: string
              supportedVerbs:
                description: SupportedVerbs at the target
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
