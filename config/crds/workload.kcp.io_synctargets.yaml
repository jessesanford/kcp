---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: synctargets.workload.kcp.io
spec:
  group: workload.kcp.io
  names:
    categories:
    - kcp
    kind: SyncTarget
    listKind: SyncTargetList
    plural: synctargets
    singular: synctarget
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.location
      name: Location
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="SyncerReady")].status
      name: Syncer
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SyncTarget represents a physical cluster that can receive workloads through
          the TMC syncer infrastructure. It defines the target cluster configuration,
          capacity limits, and workload selection criteria.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec holds the desired state of the SyncTarget.
            properties:
              clusterRef:
                description: |-
                  clusterRef references the ClusterRegistration that this SyncTarget
                  is associated with. This establishes the connection to the physical cluster.
                properties:
                  name:
                    description: name is the name of the ClusterRegistration resource.
                    type: string
                  workspace:
                    description: |-
                      workspace is the logical cluster workspace where the ClusterRegistration exists.
                      If empty, defaults to the same workspace as the SyncTarget.
                    type: string
                required:
                - name
                type: object
              location:
                description: |-
                  location is a human-readable description of where this cluster is located
                  (e.g., "us-west-2", "datacenter-1", "edge-site-a").
                type: string
              resourceQuotas:
                description: |-
                  resourceQuotas define capacity limits for this sync target.
                  If not specified, the target has unlimited capacity.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: cpu is the maximum CPU capacity that can be allocated
                      to workloads.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  custom:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: custom allows for arbitrary resource quota definitions.
                    type: object
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: memory is the maximum memory capacity that can be
                      allocated to workloads.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  pods:
                    anyOf:
                    - type: integer
                    - type: string
                    description: pods is the maximum number of pods that can be scheduled
                      on this target.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    description: storage is the maximum storage capacity that can
                      be allocated to workloads.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              selector:
                description: |-
                  selector defines criteria for selecting workloads to sync to this target.
                  If not specified, all workloads are eligible for this target.
                properties:
                  locations:
                    description: |-
                      locations is a list of location names that workloads must match.
                      If specified, only workloads with matching location annotations will be selected.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the
                      matchLabels map is equivalent to an element of matchExpressions with
                      operator "In", values containing only value.
                    type: object
                  namespaceSelector:
                    description: namespaceSelector selects workloads based on their
                      namespace labels.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              supportedResourceTypes:
                description: |-
                  supportedResourceTypes lists the Kubernetes resource types that this
                  sync target can handle. If empty, all resource types are supported.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              syncerConfig:
                description: syncerConfig contains configuration for the syncer component.
                properties:
                  retryBackoff:
                    description: retryBackoff defines retry behavior for failed sync
                      operations.
                    properties:
                      initialInterval:
                        default: 1s
                        description: initialInterval is the initial retry interval.
                        type: string
                      maxInterval:
                        default: 5m
                        description: maxInterval is the maximum retry interval.
                        type: string
                      multiplier:
                        default: 2
                        description: multiplier is the backoff multiplier.
                        type: number
                    type: object
                  syncInterval:
                    default: 30s
                    description: |-
                      syncInterval specifies how often the syncer should reconcile state.
                      Defaults to "30s".
                    type: string
                  syncMode:
                    default: push
                    description: |-
                      syncMode defines how the syncer should operate.
                      Valid values are "push" (default), "pull", or "bidirectional".
                    enum:
                    - push
                    - pull
                    - bidirectional
                    type: string
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: Status communicates the observed state of the SyncTarget.
            properties:
              allocatable:
                description: |-
                  allocatable reports the resources that are available for workload allocation
                  after accounting for system overhead.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: cpu is the CPU capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  custom:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: custom contains arbitrary resource capacity information.
                    type: object
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: memory is the memory capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  pods:
                    anyOf:
                    - type: integer
                    - type: string
                    description: pods is the pod capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    description: storage is the storage capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              allocated:
                description: allocated reports the resources currently allocated to
                  workloads.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: cpu is the CPU capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  custom:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: custom contains arbitrary resource capacity information.
                    type: object
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: memory is the memory capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  pods:
                    anyOf:
                    - type: integer
                    - type: string
                    description: pods is the pod capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    description: storage is the storage capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              capacity:
                description: capacity reports the current resource capacity available
                  on this sync target.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: cpu is the CPU capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  custom:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: custom contains arbitrary resource capacity information.
                    type: object
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: memory is the memory capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  pods:
                    anyOf:
                    - type: integer
                    - type: string
                    description: pods is the pod capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    description: storage is the storage capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              conditions:
                description: conditions is a list of conditions that apply to the
                  SyncTarget.
                items:
                  description: Condition defines an observation of a object operational
                    state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: |-
                  lastSyncTime is the last time the syncer successfully synced state
                  with the target cluster.
                format: date-time
                type: string
              supportedResourceVersions:
                additionalProperties:
                  items:
                    type: string
                  type: array
                description: |-
                  supportedResourceVersions maps resource types to their supported API versions
                  on the target cluster.
                type: object
              syncerVersion:
                description: |-
                  syncerVersion is the version of the syncer component currently running
                  for this target.
                type: string
              virtualWorkspaces:
                description: virtualWorkspaces contains URLs to virtual workspaces
                  for this sync target.
                items:
                  description: VirtualWorkspace represents a virtual workspace endpoint
                    for the sync target.
                  properties:
                    ready:
                      description: ready indicates whether the virtual workspace is
                        ready to serve requests.
                      type: boolean
                    url:
                      description: url is the virtual workspace URL.
                      minLength: 1
                      type: string
                  required:
                  - url
                  type: object
                type: array
              workloadCount:
                description: workloadCount is the number of workloads currently assigned
                  to this target.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
