apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: v250817-623e10b1.workloadstatusaggregations.workload.kcp.io
spec:
  group: workload.kcp.io
  names:
    categories:
    - kcp
    kind: WorkloadStatusAggregation
    listKind: WorkloadStatusAggregationList
    plural: workloadstatusaggregations
    singular: workloadstatusaggregation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workloadRef.name
      name: Workload
      type: string
    - jsonPath: .status.healthyLocations
      name: Healthy
      type: integer
    - jsonPath: .status.totalLocations
      name: Total
      type: integer
    - jsonPath: .status.aggregatedPhase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      description: WorkloadStatusAggregation aggregates status from multiple locations
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: WorkloadStatusAggregationSpec defines what to aggregate
          properties:
            aggregationPolicy:
              description: AggregationPolicy defines how to aggregate
              properties:
                minLocations:
                  description: MinLocations required for valid aggregation
                  format: int32
                  minimum: 1
                  type: integer
                requireAllLocations:
                  description: RequireAllLocations for aggregation
                  type: boolean
                strategy:
                  description: Strategy for aggregation
                  enum:
                  - Optimistic
                  - Pessimistic
                  - Majority
                  type: string
              required:
              - strategy
              type: object
            healthPolicy:
              description: HealthPolicy defines health criteria
              properties:
                healthyConditions:
                  description: HealthyConditions required for health
                  items:
                    description: ConditionRequirement defines a required condition
                    properties:
                      reasons:
                        description: Reason patterns to match
                        items:
                          type: string
                        type: array
                      status:
                        description: Status required
                        enum:
                        - "True"
                        - "False"
                        - Unknown
                        type: string
                      type:
                        description: Type of condition
                        type: string
                    required:
                    - status
                    - type
                    type: object
                  type: array
                minHealthyReplicas:
                  anyOf:
                  - type: integer
                  - type: string
                  description: MinHealthyReplicas required
                  x-kubernetes-int-or-string: true
                unhealthyConditions:
                  description: UnhealthyConditions that indicate problems
                  items:
                    description: ConditionRequirement defines a required condition
                    properties:
                      reasons:
                        description: Reason patterns to match
                        items:
                          type: string
                        type: array
                      status:
                        description: Status required
                        enum:
                        - "True"
                        - "False"
                        - Unknown
                        type: string
                      type:
                        description: Type of condition
                        type: string
                    required:
                    - status
                    - type
                    type: object
                  type: array
              required:
              - healthyConditions
              type: object
            statusFields:
              description: StatusFields to aggregate
              items:
                description: StatusFieldSelector selects fields to aggregate
                properties:
                  aggregationType:
                    description: AggregationType for this field
                    enum:
                    - Sum
                    - Average
                    - Min
                    - Max
                    - Latest
                    - All
                    - Count
                    type: string
                  displayName:
                    description: DisplayName for this field
                    type: string
                  path:
                    description: Path to the status field (JSONPath)
                    type: string
                  priority:
                    description: Priority for conflict resolution
                    format: int32
                    type: integer
                required:
                - aggregationType
                - path
                type: object
              type: array
            updateFrequency:
              description: UpdateFrequency for aggregation
              format: duration
              type: string
            workloadRef:
              description: WorkloadRef identifies the workload
              properties:
                apiVersion:
                  description: APIVersion of the workload
                  type: string
                kind:
                  description: Kind of the workload
                  type: string
                name:
                  description: Name of the workload
                  type: string
                namespace:
                  description: Namespace of the workload (if namespaced)
                  type: string
              required:
              - apiVersion
              - kind
              - name
              type: object
          required:
          - statusFields
          - workloadRef
          type: object
        status:
          description: WorkloadStatusAggregationStatus contains aggregated status
          properties:
            aggregatedFields:
              description: AggregatedFields with values
              items:
                description: AggregatedField contains aggregated value
                properties:
                  displayName:
                    description: DisplayName if provided
                    type: string
                  name:
                    description: Name of the field
                    type: string
                  sources:
                    description: Sources contributing to this value
                    items:
                      type: string
                    type: array
                  value:
                    description: Value after aggregation
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - name
                - value
                type: object
              type: array
            aggregatedPhase:
              description: AggregatedPhase overall phase
              enum:
              - Pending
              - Running
              - Succeeded
              - Failed
              - Unknown
              type: string
            conditions:
              description: Conditions of aggregation
              items:
                description: Condition defines an observation of a object operational
                  state.
                properties:
                  lastTransitionTime:
                    description: |-
                      Last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed. If that is not known, then using the time when
                      the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      A human readable message indicating details about the transition.
                      This field may be empty.
                    type: string
                  reason:
                    description: |-
                      The reason for the condition's last transition in CamelCase.
                      The specific API may choose whether or not this field is considered a guaranteed API.
                      This field may not be empty.
                    type: string
                  severity:
                    description: |-
                      Severity provides an explicit classification of Reason code, so the users or machines can immediately
                      understand the current situation and act accordingly.
                      The Severity field MUST be set only when Status=False.
                    type: string
                  status:
                    description: Status of the condition, one of True, False, Unknown.
                    type: string
                  type:
                    description: |-
                      Type of condition in CamelCase or in foo.example.com/CamelCase.
                      Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                      can be useful (see .node.status.conditions), the ability to deconflict is important.
                    type: string
                required:
                - lastTransitionTime
                - status
                - type
                type: object
              type: array
            healthyLocations:
              description: HealthyLocations count
              format: int32
              type: integer
            lastAggregationTime:
              description: LastAggregationTime
              format: date-time
              type: string
            locationStatuses:
              description: LocationStatuses per location
              items:
                description: AggregatedLocationStatus for a specific location
                properties:
                  extractedFields:
                    description: ExtractedFields from this location
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  healthy:
                    description: Healthy status
                    type: boolean
                  lastUpdateTime:
                    description: LastUpdateTime from this location
                    format: date-time
                    type: string
                  locationName:
                    description: LocationName
                    type: string
                  phase:
                    description: Phase at this location
                    type: string
                required:
                - healthy
                - locationName
                - phase
                type: object
              type: array
            totalLocations:
              description: TotalLocations reporting status
              format: int32
              type: integer
            unhealthyLocations:
              description: UnhealthyLocations count
              format: int32
              type: integer
          required:
          - healthyLocations
          - totalLocations
          - unhealthyLocations
          type: object
      required:
      - spec
      type: object
    served: true
    storage: true
    subresources:
      status: {}
