apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: v250804-755cf794.resourceimports.workload.kcp.io
spec:
  group: workload.kcp.io
  names:
    categories:
    - kcp
    kind: ResourceImport
    listKind: ResourceImportList
    plural: resourceimports
    shortNames:
    - ri
    singular: resourceimport
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Source cluster name
      jsonPath: .spec.source.cluster
      name: Source Cluster
      type: string
    - description: Resource type
      jsonPath: .spec.source.resource.resource
      name: Resource
      type: string
    - description: Resource namespace
      jsonPath: .spec.source.resource.namespace
      name: Namespace
      type: string
    - description: Resource name
      jsonPath: .spec.source.resource.name
      name: Name
      type: string
    - description: Ready condition status
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      description: |-
        ResourceImport represents a resource that has been imported from a SyncTarget
        into a KCP workspace for management and scheduling decisions.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: Spec defines the desired state of the ResourceImport
          properties:
            schemaUpdatePolicy:
              allOf:
              - enum:
                - UpdateAlways
                - UpdateIfNewer
                - Never
              - enum:
                - UpdateAlways
                - UpdateIfNewer
                - Never
              default: UpdateIfNewer
              description: SchemaUpdatePolicy defines how schema updates should be
                handled
              type: string
            source:
              description: Source specifies the source of the imported resource
              properties:
                cluster:
                  description: Cluster is the name of the SyncTarget where the resource
                    originates
                  type: string
                resource:
                  description: Resource specifies the resource being imported
                  properties:
                    group:
                      description: Group is the API group of the resource
                      type: string
                    name:
                      description: Name is the name of the resource
                      type: string
                    namespace:
                      description: Namespace is the namespace of the resource (if
                        namespaced)
                      type: string
                    resource:
                      description: Resource is the resource type (plural name)
                      type: string
                    version:
                      description: Version is the API version of the resource
                      type: string
                  required:
                  - name
                  - resource
                  - version
                  type: object
              required:
              - cluster
              - resource
              type: object
          required:
          - source
          type: object
        status:
          description: Status represents the current state of the ResourceImport
          properties:
            conditions:
              description: Conditions represent the latest available observations
                of the import's current state
              items:
                description: Condition defines an observation of a object operational
                  state.
                properties:
                  lastTransitionTime:
                    description: |-
                      Last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed. If that is not known, then using the time when
                      the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      A human readable message indicating details about the transition.
                      This field may be empty.
                    type: string
                  reason:
                    description: |-
                      The reason for the condition's last transition in CamelCase.
                      The specific API may choose whether or not this field is considered a guaranteed API.
                      This field may not be empty.
                    type: string
                  severity:
                    description: |-
                      Severity provides an explicit classification of Reason code, so the users or machines can immediately
                      understand the current situation and act accordingly.
                      The Severity field MUST be set only when Status=False.
                    type: string
                  status:
                    description: Status of the condition, one of True, False, Unknown.
                    type: string
                  type:
                    description: |-
                      Type of condition in CamelCase or in foo.example.com/CamelCase.
                      Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                      can be useful (see .node.status.conditions), the ability to deconflict is important.
                    type: string
                required:
                - lastTransitionTime
                - status
                - type
                type: object
              type: array
            importedResource:
              description: ImportedResource contains the actual imported resource
                data
              type: object
              x-kubernetes-preserve-unknown-fields: true
            lastImportTime:
              description: LastImportTime is when the resource was last imported
              format: date-time
              type: string
            resourceVersion:
              description: ResourceVersion is the resource version of the imported
                resource
              type: string
            schemaVersion:
              description: SchemaVersion is the version of the resource schema
              type: string
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
