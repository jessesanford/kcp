apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: v250805-30fc9e6d.trafficmetrics.tmc.kcp.io
spec:
  group: tmc.kcp.io
  names:
    categories:
    - tmc
    kind: TrafficMetrics
    listKind: TrafficMetricsList
    plural: trafficmetrics
    singular: trafficmetrics
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.metrics.successRate
      name: Success Rate
      type: string
    - jsonPath: .status.metrics.averageLatency
      name: Avg Latency
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      description: |-
        TrafficMetrics provides basic traffic analysis for TMC workload placement decisions.
        This API enables TMC to make intelligent placement decisions based on actual
        traffic performance across clusters.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: TrafficMetricsSpec defines the desired traffic metrics collection
          properties:
            clusterSelector:
              description: ClusterSelector specifies which clusters to collect metrics
                from
              properties:
                clusterNames:
                  description: ClusterNames explicitly lists cluster names to target
                  items:
                    type: string
                  type: array
                labelSelector:
                  description: LabelSelector selects clusters based on labels
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: |-
                          A label selector requirement is a selector that contains values, a key, and an operator that
                          relates the key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: |-
                              operator represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists and DoesNotExist.
                            type: string
                          values:
                            description: |-
                              values is an array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                      type: object
                  type: object
                  x-kubernetes-map-type: atomic
                locationSelector:
                  description: LocationSelector selects clusters from specific locations
                  items:
                    type: string
                  type: array
              type: object
            collectionInterval:
              description: CollectionInterval defines how often to collect metrics
              type: string
            metricsSource:
              description: MetricsSource defines where to collect traffic metrics
              properties:
                endpoint:
                  description: |-
                    Endpoint is the metrics collection endpoint URL
                    Required for Prometheus and Custom sources
                  type: string
                labels:
                  additionalProperties:
                    type: string
                  description: Labels defines additional labels to filter metrics
                  type: object
                metricsPath:
                  description: MetricsPath defines the path for metrics collection
                  type: string
                type:
                  description: Type specifies the metrics source type
                  type: string
              required:
              - type
              type: object
            retentionPeriod:
              description: RetentionPeriod defines how long to keep collected metrics
              type: string
            workloadSelector:
              description: WorkloadSelector specifies which workloads to analyze traffic
                for
              properties:
                labelSelector:
                  description: LabelSelector selects workloads based on labels
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: |-
                          A label selector requirement is a selector that contains values, a key, and an operator that
                          relates the key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: |-
                              operator represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists and DoesNotExist.
                            type: string
                          values:
                            description: |-
                              values is an array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                      type: object
                  type: object
                  x-kubernetes-map-type: atomic
                namespaceSelector:
                  description: NamespaceSelector selects workloads from specific namespaces
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: |-
                          A label selector requirement is a selector that contains values, a key, and an operator that
                          relates the key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: |-
                              operator represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists and DoesNotExist.
                            type: string
                          values:
                            description: |-
                              values is an array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                      type: object
                  type: object
                  x-kubernetes-map-type: atomic
                workloadTypes:
                  description: WorkloadTypes specifies the types of workloads to select
                  items:
                    description: WorkloadType represents a Kubernetes workload type
                    properties:
                      apiVersion:
                        description: APIVersion is the API version of the workload
                        type: string
                      kind:
                        description: Kind is the kind of the workload
                        type: string
                    required:
                    - apiVersion
                    - kind
                    type: object
                  type: array
              type: object
          required:
          - clusterSelector
          - metricsSource
          - workloadSelector
          type: object
        status:
          description: TrafficMetricsStatus defines the observed traffic metrics
          properties:
            conditions:
              description: Conditions represent the latest available observations
              items:
                description: Condition defines an observation of a object operational
                  state.
                properties:
                  lastTransitionTime:
                    description: |-
                      Last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed. If that is not known, then using the time when
                      the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      A human readable message indicating details about the transition.
                      This field may be empty.
                    type: string
                  reason:
                    description: |-
                      The reason for the condition's last transition in CamelCase.
                      The specific API may choose whether or not this field is considered a guaranteed API.
                      This field may not be empty.
                    type: string
                  severity:
                    description: |-
                      Severity provides an explicit classification of Reason code, so the users or machines can immediately
                      understand the current situation and act accordingly.
                      The Severity field MUST be set only when Status=False.
                    type: string
                  status:
                    description: Status of the condition, one of True, False, Unknown.
                    type: string
                  type:
                    description: |-
                      Type of condition in CamelCase or in foo.example.com/CamelCase.
                      Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                      can be useful (see .node.status.conditions), the ability to deconflict is important.
                    type: string
                required:
                - lastTransitionTime
                - status
                - type
                type: object
              type: array
            lastUpdateTime:
              description: LastUpdateTime indicates when metrics were last collected
              format: date-time
              type: string
            metrics:
              additionalProperties:
                description: ClusterTrafficMetrics contains traffic metrics for a
                  specific cluster
                properties:
                  averageLatency:
                    description: AverageLatency is the average request latency in
                      milliseconds
                    format: int64
                    type: integer
                  clusterName:
                    description: ClusterName identifies the cluster these metrics
                      apply to
                    type: string
                  errorCount:
                    description: ErrorCount is the number of failed requests
                    format: int64
                    type: integer
                  healthScore:
                    description: |-
                      HealthScore is a computed health score for TMC placement (0-100, higher is better)
                      Combines success rate, latency, and throughput into a single placement score
                    type: number
                  lastUpdated:
                    description: LastUpdated indicates when these metrics were last
                      collected
                    format: date-time
                    type: string
                  p95Latency:
                    description: P95Latency is the 95th percentile latency in milliseconds
                    format: int64
                    type: integer
                  requestCount:
                    description: RequestCount is the number of requests processed
                    format: int64
                    type: integer
                  successRate:
                    description: SuccessRate is the percentage of successful requests
                      (0-100)
                    type: number
                  throughput:
                    description: Throughput is requests per second
                    type: number
                required:
                - averageLatency
                - clusterName
                - errorCount
                - lastUpdated
                - requestCount
                - successRate
                - throughput
                type: object
              description: Metrics contains the current traffic metrics by cluster
              type: object
            observedWorkloads:
              description: ObservedWorkloads lists workloads currently being monitored
              items:
                description: WorkloadReference references a Kubernetes workload
                properties:
                  apiVersion:
                    description: APIVersion is the API version of the workload
                    type: string
                  kind:
                    description: Kind is the kind of the workload
                    type: string
                  name:
                    description: Name is the name of the workload
                    type: string
                  namespace:
                    description: Namespace is the namespace of the workload
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              type: array
            overallSuccessRate:
              description: OverallSuccessRate is the weighted success rate across
                all clusters (0-100)
              type: number
            phase:
              description: Phase indicates the current collection phase
              type: string
            totalRequests:
              description: TotalRequests is the sum of all requests across clusters
              format: int64
              type: integer
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
